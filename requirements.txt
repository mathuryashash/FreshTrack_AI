# Pinned to the environment that produced results/ and passes tests/ (Python 3.11-3.13).
# torch>=2.6 is also required for CVE-2025-32434 (torch.load weights_only bypass).
# Install CPU torch first so pip never pulls the CUDA wheels:
#   pip install torch==2.12.0 torchvision==0.27.0 --index-url https://download.pytorch.org/whl/cpu

# Core ML
torch==2.12.0
torchvision==0.27.0
pytorch-lightning==2.6.1
timm==1.0.24  # >=1.0 for head_hidden_size (MobileNetV3 feature width)

# Data & Image Processing
albumentations==2.0.8  # dataset.py uses the 2.x CoarseDropout API
opencv-python==4.12.0.88
numpy==2.2.6
pandas==2.2.3
pillow==12.2.0
scikit-learn==1.7.2
python-magic==0.4.27

# API
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.32
slowapi==0.1.9
psutil==6.0.0

# Frontend
streamlit==1.54.0

# MLOps & Experiment Tracking
wandb==0.24.2
mlflow==3.9.0
dvc==3.66.1
evidently==0.7.20

# Produce detector (src/detection): COCO-style AP. Its data stages that
# auto-label and cut out items also need transformers (Grounding DINO, SAM):
#   pip install transformers==4.57.6
pycocotools==2.0.11

# Utilities
python-dotenv==1.0.1
matplotlib==3.10.7
kaggle==2.0.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.27.2
